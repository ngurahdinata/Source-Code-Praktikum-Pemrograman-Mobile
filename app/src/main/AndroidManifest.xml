<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sipon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SiPon">
        <activity android:name=".UpdateActivity"></activity>
        <activity android:name=".List_Member" />
        <activity
            android:name=".Member"
            android:label="@string/title_activity_member"
            android:theme="@style/Theme.SiPon.NoActionBar" />
        <activity android:name=".detail_member" />
        <activity android:name=".tambah_member" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>